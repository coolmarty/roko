#include "drone.h"
#include <iostream>


int main(){
	Drone basilisk = Drone(/*Point3(20, 0, 50), Vector3(0, 0, 0), Vector3(0, 0, 0), 0, 0, 0*/);
	// float time = 0;
	// while(1){
	// 	Point3 position = basilisk.GetPosition();
	// 	std::cout << time << ": " << position.GetX() << ", " << position.GetY() << ", " << position.GetZ() << "\n";
	// 	basilisk.Update(0.01);
	// 	time += 0.01;
	// }
}